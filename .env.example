# Dex MCP Server Configuration Example
# Copy this file to .env and fill in your values

# Required: Dex API base URL
DEX_API_URL=https://api.getdex.com/api/rest

# Required: Dex API authentication key
DEX_API_KEY=your-api-key-here

# Optional: Cache TTL in minutes (default: 30)
# Used for both contact cache and search index cache
# DEX_SEARCH_CACHE_TTL_MINUTES=30

# Optional: HTTP request timeout in seconds (default: 10)
# REQUEST_TIMEOUT=10

# Optional: Maximum number of fuzzy match results to return (default: 5)
# MAX_MATCH_RESULTS=5

# Optional: Fuzzy match confidence threshold 0-100 (default: 30)
# MATCH_CONFIDENCE_THRESHOLD=30

# Optional: Log level - error, warn, info, debug, trace (default: error)
# LOG_LEVEL=error
